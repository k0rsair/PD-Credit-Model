# ML Проект: Скоринговая модель предсказания дефолта

Проект для построения и развертывания ML-моделей предсказания дефолта клиентов на основе данных кредитных карт. Проект использует современные инструменты MLOps: DVC для управления данными и пайплайнами, MLflow для отслеживания экспериментов, pytest для тестирования.

## 🎯 Описание проекта

Проект реализует полный ML-пайплайн для предсказания вероятности дефолта клиента на основе исторических данных о платежах по кредитным картам. Пайплайн включает:

- **Подготовку данных**: очистка, обработка пропусков и аномалий
- **Feature Engineering**: создание новых признаков (веса платежей, биннинг возраста, отношения выплат к счетам)
- **Обучение моделей**: сравнение нескольких алгоритмов машинного обучения
- **Гиперпараметрическая оптимизация**: автоматический подбор параметров
- **Оценка качества**: метрики ROC-AUC, F1-score, precision, recall
- **Визуализация**: ROC-кривые для каждой модели

## 📁 Структура проекта

```
├── data/                          # Данные проекта
│   ├── raw/                       # Исходные данные
│   │   └── UCI_Credit_Card.csv    # Датасет UCI Credit Card
│   └── processed/                 # Обработанные данные
│       ├── prepared.csv           # Подготовленные данные
│       └── featured.csv           # Данные с фичами
│
├── scripts/                       # Скрипты проекта
│   ├── data/                      # Скрипты обработки данных
│   │   ├── prepare_data.py        # Подготовка данных
│   │   ├── feature_engineering.py # Создание признаков
│   │   └── validate_data.py       # Валидация данных
│   └── models/                    # Скрипты обучения моделей
│       ├── train_model.py         # Обучение моделей
│       ├── tuning.py              # Гиперпараметрическая оптимизация
│       ├── preprocessing.py       # Препроцессинг данных
│       └── utils.py              # Утилиты (метрики, логирование)
│
├── models/                        # Сохраненные модели
│   ├── LogisticRegression.pkl
│   ├── RandomForest.pkl
│   ├── GradientBoosting.pkl
│   ├── XGBoost.pkl
│   └── LightGBM.pkl
│
├── tests/                         # Тесты
│   ├── conftest.py               # Конфигурация pytest
│   ├── test_data_validation.py   # Тесты валидации данных
│   ├── test_integrationals.py    # Интеграционные тесты
│   └── unit/                     # Юнит-тесты
│
├── docker/                        # Docker конфигурация
│   ├── nginx/                    # Nginx конфигурация
│   └── python/                   # Python API сервис
│
├── mlruns/                        # MLflow эксперименты
├── artifacts/                     # Артефакты (графики, отчеты)
├── notebooks/                     # Jupyter ноутбуки
│
├── dvc.yaml                       # DVC пайплайн
├── dvc.lock                       # DVC lock файл
├── requirements.txt               # Python зависимости
├── pytest.ini                     # Конфигурация pytest
└── docker-compose.yml             # Docker Compose конфигурация
```

## ⚙️ Установка и настройка

### Предварительные требования

- Python 3.8+
- Git
- DVC
- Docker и Docker Compose (опционально, для развертывания)

### Установка зависимостей

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd track-2
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Для Linux/Mac
# или
venv\Scripts\activate  # Для Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Установите DVC (если еще не установлен):
```bash
pip install dvc
```

Для локальной работы DVC будет использовать локальное хранилище.

## 🚀 Запуск пайплайна

### Полный пайплайн через DVC

Запуск всего пайплайна от подготовки данных до обучения моделей:

```bash
dvc repro
```

Это выполнит все этапы в правильном порядке:
1. **prepare** - подготовка данных
2. **validate** - валидация данных
3. **feature_engineering** - создание признаков
4. **train** - обучение моделей



### Запуск пайплайна
```bash
dvc repro
```


## 🎓 Обучение моделей

Проект обучает и сравнивает следующие модели:

- **Logistic Regression** - логистическая регрессия
- **Random Forest** - случайный лес
- **Gradient Boosting** - градиентный бустинг
- **XGBoost** - оптимизированный градиентный бустинг
- **LightGBM** - легковесный градиентный бустинг

### Процесс обучения

1. Данные разделяются на train/test (80/20) с сохранением стратификации
2. Для каждой модели выполняется:
   - Препроцессинг данных (стандартизация, обработка категориальных признаков)
   - Гиперпараметрическая оптимизация через RandomizedSearchCV
   - Обучение на train выборке
   - Оценка на test выборке
   - Логирование в MLflow (метрики, параметры, ROC-кривая, модель)

3. Модели сохраняются в директорию `models/`

### Метрики

Для каждой модели вычисляются:
- **ROC-AUC** - площадь под ROC-кривой
- **F1-score** - гармоническое среднее precision и recall
- **Precision** - точность
- **Recall** - полнота
- **ROC-кривая** - визуализация качества модели

## 🧪 Тестирование

Запуск всех тестов:

```bash
pytest
```

Запуск с подробным выводом:

```bash
pytest -v
```


## 🐳 Развертывание

### Запуск через Docker Compose

1. Соберите и запустите контейнеры:
```bash
docker-compose up --build
```

2. API будет доступно на:
   - **Nginx**: http://localhost:80
   - **Python API**: http://localhost:5001

3. Остановка контейнеров:
```bash
docker-compose down
```

### Использование API

Пример запроса к API для предсказания:

```bash
curl -X POST http://localhost:5001/predict \
  -H "Content-Type: application/json" \
  -d '{"features": [...]}'
```

## 📊 MLflow UI

Для просмотра экспериментов и сравнения моделей:

1. Запустите MLflow UI:
```bash
mlflow ui --backend-store-uri file:./mlruns
```

2. Откройте в браузере:
```
http://localhost:5000
```

В MLflow UI вы можете:
- Просматривать все эксперименты
- Сравнивать метрики моделей
- Просматривать ROC-кривые
- Скачивать обученные модели
- Просматривать гиперпараметры

## 🔍 Дополнительная информация

### Данные

Проект использует датасет **UCI Credit Card Default Dataset**, который содержит информацию о:
- Демографических характеристиках клиентов
- Истории платежей (PAY_0 - PAY_6)
- Суммах счетов (BILL_AMT1 - BILL_AMT6)
- Суммах платежей (PAY_AMT1 - PAY_AMT6)
- Целевой переменной: дефолт в следующем месяце

### Feature Engineering

Создаются следующие признаки:
- **PAY_WEIGHT** - вес, растущий за своевременные платежи
- **AGE_BINNED** - биннинг возраста (20-30, 30-40, 40-50, 50-60, 60-80)
- **BILL_TOTAL** - сумма всех счетов
- **PAY_TOTAL** - сумма всех платежей
- **PAY_RATIO** - отношение платежей к счетам


